<h1>My Song</h1>
<p><%= @song.name %></p>
<% if @song.artist %>
  <p><%= link_to @song.artist.name, artist_path(@song.artist_id) %></p>
<% end %>
<% if @song.genre %>
  <p><%= link_to @song.genre.name, genre_path(@song.genre_id) %></p>
<% end %>
